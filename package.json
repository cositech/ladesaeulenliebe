{
  "name": "ladesaeulenliebe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free Cloudflare Worker microsite",
  "scripts": {
    "build": "node -e \"console.log('Static assets: no build required')\"",
    "check": "node --check worker/index.js",
    "dev": "npx --yes wrangler@latest dev",
    "deploy": "npx --yes wrangler@latest deploy"
  }
}
